# Set update schedule for GitHub Actions

version: 2
updates:

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      # Check for updates to GitHub Actions every week
      interval: "weekly"
    commit-message:
      prefix: infra

  - package-ecosystem: "pip"
    # Files stored in repository root
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      - dependency-name: "jax"
        versions: [ ">0.6.2" ]  # JAX >0.6.2 not compatible with PennyLane
      - dependency-name: "jaxlib"
        versions: [ ">0.6.2" ]  # JAX >0.6.2 not compatible with PennyLane
      - dependency-name: "pandas"
        versions: [ ">=3.0.0" ]  # pandas >=3.0.0 not compatible with JAX 0.6.2
      - dependency-name: "numpy"
        versions: [ ">=2.3.0" ]
    commit-message:
      prefix: infra
    groups:
       # Specify a name for the group, which will be used in pull request titles
       # and branch names
       dev-dependencies:
          # Define patterns to include dependencies in the group (based on
          # dependency name)
          patterns:
            - "*"       # A wildcard that matches all dependencies in the package
                        # ecosystem. Note: using "*" may open a large pull request

  - package-ecosystem: "pip"
    directory: "/extra"
    schedule:
      interval: "monthly"
    ignore:
      - dependency-name: "jax"
        versions: [ ">0.6.2" ]  # JAX >0.6.2 not compatible with PennyLane
      - dependency-name: "jaxlib"
        versions: [ ">0.6.2" ]  # JAX >0.6.2 not compatible with PennyLane
      - dependency-name: "numpy"
        versions: [ ">=2.3.0" ]
    commit-message:
      prefix: infra
    groups:
       dev-dependencies:
          patterns:
            - "*"
